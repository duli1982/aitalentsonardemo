import React from 'react';
import { CheckCircle2 } from 'lucide-react';
import { Link, useLocation, useParams } from 'react-router-dom';
import { CareerShell } from './CareerSitePage';
const CareerConfirmationPage: React.FC = () => { const { organization = 'local-workspace' } = useParams(); const state = useLocation().state as { jobTitle?: string; duplicateReview?: boolean } | null; return <CareerShell organization={organization} name="Talent Sonar"><div className="mx-auto max-w-xl py-24 text-center"><CheckCircle2 className="mx-auto h-14 w-14 text-emerald-600" /><h1 className="mt-5 text-4xl font-black">Application received</h1><p className="mt-3 text-lg text-slate-600">Thank you{state?.jobTitle ? ` for applying for ${state.jobTitle}` : ''}. Your information is now available for recruiter review.</p>{state?.duplicateReview && <p className="mt-5 rounded-xl bg-amber-50 p-4 text-sm text-amber-900">We found an earlier application with this email. A recruiter will review the records together; no automatic rejection occurred.</p>}<p className="mt-5 text-sm leading-6 text-slate-500">The assigned recruiter has received a new-application task. Keep this confirmation page for your records.</p><Link to={`/careers/${organization}`} className="mt-7 inline-flex rounded-xl bg-sky-600 px-5 py-3 font-black text-white">View more opportunities</Link></div></CareerShell>; };
export default CareerConfirmationPage;
